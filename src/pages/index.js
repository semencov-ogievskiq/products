import React from 'react'
import { connect } from 'react-redux'

const Index = ( props ) => {
    console.log( props )

  
    return (
        <div>
            dfdftrt
        </div>
    )

}

export default connect()(Index)